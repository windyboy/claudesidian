---
name: 架构设计修复计划
overview: 根据评审报告的24个问题，系统性地修复 OPENCODE-ARCHITECTURE-DESIGN.md 中的设计缺陷，优先处理高严重度问题（P01-P08），然后改进中低严重度问题。
todos:
  - id: fix-p01
    content: 修复职责边界定义（P01）- 明确 Obsidian 层和 OpenCode Plugin 层的职责，迁移业务逻辑到 Plugin 层
    status: pending
  - id: fix-p02
    content: 添加状态同步机制设计（P02）- 定义状态分类、同步策略和冲突解决机制
    status: pending
  - id: fix-p03
    content: 定义状态所有权模型（P03）- 为每个状态定义所有者和访问权限
    status: pending
    dependencies:
      - fix-p02
  - id: fix-p04
    content: 完善事件类型定义（P04）- 使用命名空间组织所有事件类型，确保类型安全
    status: pending
  - id: fix-p05
    content: 添加错误处理策略设计（P05）- 定义错误分类、重试策略和降级机制
    status: pending
  - id: fix-p06
    content: 修复危险命令检测机制（P06）- 从黑名单改为白名单，添加命令解析器
    status: pending
  - id: fix-p07
    content: 添加路径保护符号链接处理（P07）- 解析符号链接，添加路径验证和 Jail 机制
    status: pending
  - id: fix-p08
    content: 定义 MCP 服务器沙箱边界（P08）- 添加沙箱配置、资源限制和监控机制
    status: pending
  - id: improve-p09
    content: 定义 Plugin 间依赖关系（P09）- 为每个 Plugin 声明依赖，实现依赖解析
    status: pending
    dependencies:
      - fix-p01
  - id: improve-p10
    content: 添加 Vault 搜索索引机制（P10）- 实现索引构建、更新和搜索优化
    status: pending
  - id: improve-p11
    content: 添加事件流背压控制（P11）- 实现缓冲区管理和流控机制
    status: pending
  - id: improve-p12
    content: 优化图片处理为异步（P12）- 确保图片处理不阻塞 UI 线程
    status: pending
    dependencies:
      - fix-p01
  - id: improve-p13
    content: 优化 getVaultInfo 性能（P13）- 添加缓存和增量更新机制
    status: pending
  - id: improve-p14
    content: 添加配置验证机制（P14）- 定义配置 Schema 和验证器
    status: pending
  - id: improve-p15
    content: 添加结构化日志和指标（P15）- 定义日志格式和关键指标
    status: pending
  - id: improve-p16-p18
    content: 完善迁移方案（P16-P18）- 添加渐进式迁移路径、回滚策略和数据迁移脚本
    status: pending
  - id: polish-p19
    content: 统一工具返回值结构（P19）- 定义标准返回值接口
    status: pending
  - id: polish-p20
    content: 添加 API 版本控制（P20）- 定义版本策略和兼容性规则
    status: pending
  - id: polish-p21
    content: 添加时序图和交互流程（P21）- 为关键流程添加 Mermaid 时序图
    status: pending
  - id: polish-p22
    content: 添加测试策略（P22）- 定义测试层级和覆盖率目标
    status: pending
  - id: polish-p23
    content: 明确自定义工具注册机制（P23）- 定义工具注册流程和 API
    status: pending
  - id: polish-p24
    content: 添加国际化支持（P24）- 定义错误消息国际化机制
    status: pending
---

#架构设计修复计划基于评审报告的24个问题，本计划将系统性地修复 OPENCODE-ARCHITECTURE-DESIGN.md 中的设计缺陷。

## 修复策略

### 阶段一：高严重度问题修复（P01-P08）

优先修复架构、状态管理和安全性问题，这些是系统稳定性的基础。

### 阶段二：中严重度问题改进（P09-P18）

改进性能、配置管理和迁移方案，提升系统质量和可维护性。

### 阶段三：低严重度问题完善（P19-P24）

完善API设计、文档和测试策略，提升开发体验。---

## 阶段一：高严重度问题修复

### 任务 1.1: 修复职责边界定义（P01）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"3.2 职责划分"章节后新增"3.2.1 层级职责详细定义"子章节
2. 添加 `LayerResponsibilities` 接口定义，明确每层的 allowed/forbidden 操作
3. 修改"2.3.2 图片上下文管理"章节：

- 将图片编码逻辑从 Obsidian 层迁移到 OpenCode Plugin 层
- 修改为：Obsidian 仅发送文件路径，OpenCode Plugin 负责读取和编码

4. 修改"2.3.1 文件上下文管理"章节：

- 将 `ContextPathScanner` 从 Obsidian 层迁移到 OpenCode Plugin 层
- 修改为：Obsidian 仅触发扫描请求，OpenCode Plugin 执行扫描逻辑

5. 修改"2.3.4 权限管理"章节：

- 明确权限规则管理和权限持久化完全在 OpenCode Plugin 层
- Obsidian 层仅负责审批 UI 展示

**验收标准**:

- 所有业务逻辑明确标注在 OpenCode Plugin 层
- Obsidian 层职责清单中无业务逻辑处理

---

### 任务 1.2: 添加状态同步机制设计（P02）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"3.2 职责划分"章节后新增"3.3 状态管理设计"章节
2. 定义状态分类体系：

- `StateCategory`: ServerAuthoritative, ClientLocal, Bidirectional, Derived

3. 定义状态注册表 `STATE_REGISTRY`，包含：

- 状态键名
- 状态类别
- 同步策略（pull/push/push_pull，触发条件，同步间隔）
- 冲突解决策略（server_wins/client_wins/last_write_wins/merge）

4. 实现 `StateSyncManager` 类设计：

- `getState()` / `setState()` 方法
- `handleServerStateUpdate()` 方法
- `resolveConflict()` 方法
- 事件监听和定时同步机制

5. 定义状态持久化策略：

- 存储位置（memory/localStorage/indexedDB/file）
- 加密策略
- TTL 和最大条目数

**验收标准**:

- 所有状态都在 STATE_REGISTRY 中注册
- 每个状态都有明确的同步策略和冲突解决策略

---

### 任务 1.3: 定义状态所有权模型（P03）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"3.3 状态管理设计"章节中新增"3.3.1 状态所有权定义"子章节
2. 定义 `StateOwnership` 接口：

- `owner`: 状态所有者（obsidian/opencode_client/opencode_server/opencode_plugin）
- `readers`: 可读取状态的实体列表
- `mutationMethod`: 修改方式（direct/request/event）
- `notificationMethod`: 通知方式（push/pull/none）

3. 创建 `STATE_OWNERSHIP` 注册表，为每个状态定义所有权：

- session.* 状态 → opencode_server 拥有
- plan.* 状态 → opencode_plugin 拥有
- context.* 状态 → obsidian 拥有
- client.* 状态 → opencode_client 拥有

4. 实现 `StateGuard` 类设计：

- `canMutate()` / `canRead()` 方法
- `mutate()` 方法（强制执行所有权）

5. 定义跨实体状态请求协议：

- `StateMutationRequest` / `StateMutationResponse` 接口
- 非所有者修改状态的请求流程

**验收标准**:

- 每个状态都有明确的所有者定义
- 非所有者无法直接修改状态

---

### 任务 1.4: 完善事件类型定义（P04）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 修改"3.3.2 事件类型"章节（原"3.3.2 通信协议"中的事件类型部分）
2. 使用命名空间组织事件类型：

- `OpenCodeEvents.Session.*` (Created, Resumed, Ended, StateChanged, ContextUpdated)
- `OpenCodeEvents.Message.*` (BeforeSend, Sent, StreamStarted, StreamChunk, StreamEnded, Updated, Error)
- `OpenCodeEvents.Tool.*` (BeforeUse, Use, Progress, Result, Error)
- `OpenCodeEvents.Permission.*` (Requested, Granted, Denied, RuleChanged)
- `OpenCodeEvents.Plan.*` (Created, StepStarted, StepCompleted, StepFailed, Completed, Cancelled)
- `OpenCodeEvents.MCP.*` (ServerStarting, ServerStarted, ServerStopped, ServerError, ToolCalled, ToolResult)
- `OpenCodeEvents.Error.*` (Occurred, Recovered)
- `OpenCodeEvents.Connection.*` (Connecting, Connected, Disconnected, Reconnecting)

3. 为每个事件类型定义完整的 payload 结构
4. 创建类型安全的事件处理器 `TypedEventEmitter`
5. 更新所有使用事件的地方，确保使用正确的事件类型：

- 修改"4.4 Slash Commands Plugin"中的 `message.before_send` 事件
- 修改"4.5 MCP Router Plugin"中的 `mcp.server.started` 和 `mcp.server.stopped` 事件

**验收标准**:

- 所有实际使用的事件都有类型定义
- 事件类型支持 TypeScript 类型推断

---

### 任务 1.5: 添加错误处理策略设计（P05）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"3.3 状态管理设计"章节后新增"3.4 错误处理设计"章节
2. 定义错误分类体系：

- `ErrorCategory`: Network, Authentication, Validation, Resource, Tool, Permission, Server, ExternalService, Unknown
- `ErrorSeverity`: Ignorable, Warning, Error, Fatal
- `ErrorRecoverability`: AutoRecoverable, UserRecoverable, Unrecoverable

3. 定义结构化错误接口 `StructuredError`：

- id, code, category, severity, recoverability
- message, details, cause, suggestedActions, timestamp, context

4. 定义重试策略 `RetryPolicy`：

- maxAttempts, backoff (fixed/linear/exponential), retryableCategories, retryableCodes

5. 定义降级策略 `FallbackPolicy`：

- enabled, fallbackMethod (cache/default/alternative/skip/custom)

6. 为每个错误类别定义默认错误处理配置 `DEFAULT_ERROR_CONFIGS`
7. 实现 `ErrorHandler` 类设计：

- `withErrorHandling()` 方法（包装异步操作）
- `normalizeError()` 方法（规范化错误）
- `shouldRetry()` / `calculateDelay()` 方法
- `executeFallback()` 方法

**验收标准**:

- 所有错误类别都有明确的处理策略
- 网络错误、工具错误等关键场景有重试和降级机制

---

### 任务 1.6: 修复危险命令检测机制（P06）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 修改"4.2 Permission Manager Plugin"章节中的权限规则部分
2. 移除基于正则表达式的黑名单机制
3. 实现白名单机制：

- 定义 `ALLOWED_COMMANDS` 白名单（仅允许预定义的安全命令）
- 所有不在白名单中的命令都需要用户审批

4. 添加命令解析器：

- 使用 AST 解析器或命令参数解析器，而不是简单的正则匹配
- 检测命令参数分离、长参数、编码绕过等攻击

5. 强制所有 `bash` 工具执行都经过用户审批（Always Ask）
6. 添加沙箱执行选项（可选）：

- 在 Docker/WASM 沙箱中运行危险命令

7. 更新权限规则示例代码，展示白名单机制

**验收标准**:

- 不再使用正则表达式黑名单
- 所有 bash 命令执行都有明确的审批流程

---

### 任务 1.7: 添加路径保护符号链接处理（P07）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 修改"4.2 Permission Manager Plugin"章节中的路径保护部分
2. 在 `protectedPaths` 检查后，添加符号链接解析：

- 使用 `fs.realpath()` 或 `path.resolve()` 解析符号链接
- 检查解析后的真实路径是否在保护路径中

3. 添加路径规范化函数：

- 使用 `path.relative()` 检查路径是否在 Vault 根目录内（Jail）
- 禁止访问 Vault 根目录以外的文件

4. 添加路径验证工具函数：
   ```typescript
                        function validatePath(filePath: string, vaultRoot: string): boolean {
                          const resolved = path.resolve(vaultRoot, filePath);
                          const realPath = fs.realpathSync(resolved);
                          return realPath.startsWith(vaultRoot) && !isProtectedPath(realPath);
                        }
   ```




5. 更新所有文件操作工具（vault_read_file, vault_write_file）使用路径验证

**验收标准**:

- 所有路径验证都解析符号链接
- 禁止访问 Vault 根目录以外的文件

---

### 任务 1.8: 定义 MCP 服务器沙箱边界（P08）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"4.5 MCP Router Plugin"章节中新增"4.5.1 MCP 服务器沙箱配置"子章节
2. 定义 MCP 服务器沙箱配置接口：

- `sandboxType`: 'none' | 'chroot' | 'docker' | 'wasm'
- `allowedPaths`: 允许访问的路径列表
- `allowedCommands`: 允许执行的命令列表
- `resourceLimits`: CPU、内存、网络限制

3. 在 `opencode.jsonc` 配置中添加 MCP 沙箱配置示例
4. 定义 MCP 服务器隔离策略：

- 文件系统访问限制
- 网络访问限制
- 环境变量隔离

5. 添加 MCP 服务器监控和审计日志：

- 记录所有工具调用
- 记录资源使用情况
- 记录异常行为

**验收标准**:

- 每个 MCP 服务器都有明确的沙箱配置
- 有资源限制和访问控制机制

---

## 阶段二：中严重度问题改进

### 任务 2.1: 定义 Plugin 间依赖关系（P09）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"4. 功能点详细设计"章节开头新增"4.0 Plugin 依赖管理"子章节
2. 定义 Plugin 依赖配置：

- `dependencies`: 依赖的插件列表
- `loadOrder`: 加载顺序（如果未指定依赖，按此顺序加载）

3. 为每个 Plugin 添加依赖声明：

- vault-context.ts: 无依赖
- permission-manager.ts: 无依赖
- plan-mode.ts: 依赖 permission-manager
- slash-commands.ts: 无依赖
- mcp-router.ts: 无依赖
- session-manager.ts: 依赖 vault-context
- image-processor.ts: 无依赖

4. 定义 Plugin 加载器设计：

- 依赖解析算法（拓扑排序）
- 循环依赖检测
- 加载失败处理

**验收标准**:

- 所有 Plugin 都有明确的依赖声明
- 有依赖解析和加载顺序机制

---

### 任务 2.2: 添加 Vault 搜索索引机制（P10）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 修改"4.1 Vault Context Plugin"章节中的 `vault_search_content` 工具
2. 添加索引管理工具：

- `vault_index_build`: 构建搜索索引
- `vault_index_update`: 增量更新索引

3. 定义索引存储策略：

- 索引文件位置（`.opencode/index/`）
- 索引格式（倒排索引、全文索引）

4. 添加索引更新机制：

- 文件变更时自动更新索引
- 定期重建索引

5. 优化搜索性能：

- 使用索引加速搜索
- 支持模糊搜索、正则搜索

**验收标准**:

- 有索引构建和更新机制
- 搜索性能显著提升

---

### 任务 2.3: 添加事件流背压控制（P11）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"3.3.2 通信协议"章节中新增"3.3.2.1 事件流背压控制"子章节
2. 定义背压控制策略：

- 缓冲区大小限制
- 流控信号（pause/resume）
- 背压阈值（当缓冲区达到 80% 时暂停）

3. 实现背压控制机制：

- 客户端发送 `flow_control: pause` 信号
- 服务器暂停发送事件
- 客户端处理完成后发送 `flow_control: resume` 信号

4. 添加事件流监控：

- 缓冲区使用率
- 事件处理延迟
- 背压触发次数

**验收标准**:

- 有明确的背压控制机制
- 防止事件流溢出导致内存问题

---

### 任务 2.4: 优化图片处理为异步（P12）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 修改"2.3.2 图片上下文管理"章节
2. 明确图片处理为异步操作：

- Obsidian 层：异步发送图片路径到 OpenCode
- OpenCode Plugin 层：在后台线程/Worker 中处理图片编码

3. 添加图片处理进度事件：

- `image.processing_started`
- `image.processing_progress`
- `image.processing_completed`

4. 优化大图片处理：

- 图片压缩（在编码前）
- 分块传输（如果图片很大）
- 使用文件路径而非 Base64（同机器时）

**验收标准**:

- 图片处理不阻塞 UI 线程
- 有进度反馈机制

---

### 任务 2.5: 优化 getVaultInfo 性能（P13）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 修改"4.6 Session Manager Plugin"章节中的 `getVaultInfo` 函数
2. 添加缓存机制：

- 缓存 Vault 结构信息（TTL: 5分钟）
- 文件变更时失效缓存

3. 优化遍历逻辑：

- 使用增量扫描（仅扫描变更的文件）
- 并行扫描多个文件夹

4. 添加懒加载：

- 首次仅加载基本信息
- 按需加载详细结构

5. 添加索引文件：

- 维护 `.opencode/vault-index.json` 文件
- 定期更新索引

**验收标准**:

- getVaultInfo 响应时间 < 100ms（缓存命中时）
- 支持增量更新

---

### 任务 2.6: 添加配置验证机制（P14）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"6. 配置管理"章节中新增"6.1.1 配置验证"子章节
2. 定义配置 Schema（基于 JSON Schema）：

- 使用 `$schema` 字段引用 Schema
- 定义所有配置项的类型、必填项、默认值、取值范围

3. 实现配置验证器：

- 启动时验证 `opencode.jsonc` 配置
- 配置更新时验证
- 提供详细的验证错误信息

4. 添加配置迁移工具：

- 自动迁移旧版本配置到新版本
- 废弃配置项警告

**验收标准**:

- 有完整的配置 Schema 定义
- 配置错误有清晰的提示信息

---

### 任务 2.7: 添加结构化日志和指标（P15）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"3. 架构设计"章节中新增"3.5 可观测性设计"章节
2. 定义日志级别和格式：

- 日志级别：DEBUG, INFO, WARN, ERROR, FATAL
- 结构化日志格式（JSON）
- 日志字段：timestamp, level, category, message, context, error

3. 定义关键指标：

- 请求指标：请求数、延迟、错误率
- 工具指标：工具调用数、成功率、平均耗时
- 资源指标：内存使用、CPU 使用、事件流缓冲区大小

4. 实现日志和指标收集：

- 日志输出到文件和控制台
- 指标导出到 Prometheus 格式（可选）

5. 添加性能监控点：

- 关键操作添加性能计时
- 慢查询检测

**验收标准**:

- 有完整的日志和指标定义
- 关键操作都有监控点

---

### 任务 2.8: 完善迁移方案（P16-P18）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 修改"7. 迁移方案"章节
2. 添加"7.1.1 渐进式迁移路径"子章节：

- 定义迁移阶段（4个阶段，每个阶段可独立部署）
- 每个阶段的验收标准
- 阶段间的兼容性保证

3. 完善"7.3 回滚方案"章节：

- 详细的回滚步骤
- 数据备份和恢复机制
- 回滚验证清单

4. 新增"7.4 数据迁移脚本"章节：

- 对话历史迁移脚本
- 配置迁移脚本
- 权限规则迁移脚本
- 斜杠命令迁移脚本

5. 添加迁移工具：

- 迁移前检查清单
- 自动迁移脚本
- 迁移验证工具

**验收标准**:

- 有详细的渐进式迁移路径
- 有完整的回滚和数据迁移方案

---

## 阶段三：低严重度问题完善

### 任务 3.1: 统一工具返回值结构（P19）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"附录 B. API 参考"章节中新增"B.1 工具返回值标准"子章节
2. 定义标准工具返回值接口：
   ```typescript
                        interface StandardToolResult {
                          success: boolean;
                          data?: any;
                          error?: {
                            code: string;
                            message: string;
                            details?: any;
                          };
                          metadata?: {
                            duration?: number;
                            warnings?: string[];
                          };
                        }
   ```




3. 更新所有工具定义，使用标准返回值结构
4. 添加返回值验证机制

---

### 任务 3.2: 添加 API 版本控制（P20）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"3.3.2 通信协议"章节中添加 API 版本控制
2. 定义版本策略：

- URL 路径版本控制：`/v1/session/create`
- 请求头版本控制：`X-API-Version: 1.0`

3. 定义版本兼容性策略：

- 向后兼容规则
- 废弃 API 的迁移路径

4. 添加版本检查机制

---

### 任务 3.3: 添加时序图和交互流程（P21）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"5. 数据流与交互"章节中添加 Mermaid 时序图
2. 为关键流程添加时序图：

- 用户查询流程（详细版）
- 权限审批流程（详细版）
- 计划模式流程（详细版）
- 状态同步流程
- 错误处理流程

---

### 任务 3.4: 添加测试策略（P22）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"8. 实施计划"章节后新增"9. 测试策略"章节
2. 定义测试层级：

- 单元测试（Plugin 功能测试）
- 集成测试（Plugin 间交互测试）
- 端到端测试（Obsidian + OpenCode 完整流程）

3. 定义测试工具和框架
4. 定义测试覆盖率目标

---

### 任务 3.5: 明确自定义工具注册机制（P23）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"附录 B. API 参考"章节中新增"B.2 自定义工具注册"子章节
2. 定义工具注册流程：

- 工具定义格式
- 注册 API
- 工具发现机制

3. 添加工具注册示例

---

### 任务 3.6: 添加国际化支持（P24）

**文件**: `OPENCODE-ARCHITECTURE-DESIGN.md`**修改内容**:

1. 在"3. 架构设计"章节中新增"3.6 国际化设计"子章节
2. 定义错误消息国际化机制：

- 错误消息键值对
- 多语言支持
- 默认语言（英语）

3. 添加国际化配置

---

## 实施顺序

1. **Week 1-2**: 阶段一任务（P01-P08）- 高严重度问题修复
2. **Week 3-4**: 阶段二任务（P09-P18）- 中严重度问题改进